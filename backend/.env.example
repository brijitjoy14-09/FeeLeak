# FeeLeak backend configuration (placeholders only — never commit real secrets).
#
# AI investigation/insight provider. Only "mock" ships in this MVP (deterministic,
# no external calls, no credentials required). A real provider would read its
# key/model from these variables.
AI_PROVIDER=mock
AI_API_KEY=
AI_MODEL=mock-model
AI_TIMEOUT_SECONDS=20
